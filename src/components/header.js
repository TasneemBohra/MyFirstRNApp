// Import libs for making a Component
import React from 'react';
import { Text } from 'react-native'; // Destructured import

// Make a Component
const Header = () => {
  return (
    <Text>Almus</Text>
  );
};

// Make the component available to other parts of the app
export default Header;
